<nav class="navbar navbar-expand-lg navbar-light bg-transparent mb-2 bd-subnavbar">
    <div class="container-fluid px-3">
        <a class="navbar-brand" href="{{ path('home') }}"><i><b>Adventure</b></i></a>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('home') }}">Accueil</a>
                </li>

                {% if is_granted('ROLE_SUPER_ADMIN') %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('user') }}">Users</a>
                    </li>
                {% endif %}
            </ul>

            <ul class="navbar-nav">

                {% if is_granted('ROLE_USER') %}
                    <li class="nav-item">
                        <span class="nav-link py-1">
                        <i class="bi bi-person-circle" style="font-size: 1.1rem;"></i>
                        </span>
                    </li>
                    <li class="nav-item">
                        <a href="/logout" class="nav-link py-1" data-bs-toggle="popover" data-bs-trigger="hover focus" data-bs-content="Se déconnecter" data-bs-placement="bottom">
                            <i class="bi bi-box-arrow-right" style="font-size: 1.1rem;"></i>
                        </a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>